# Shared contract fixtures — review required

Contains golden request and error examples for contract consumers. Fixtures use synthetic identifiers and locations and must never contain secrets or personal data.
