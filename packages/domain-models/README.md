# Shared domain models — review required

Provider-neutral v1 models currently live beside the API in `apps/api/src/advisor_api/contracts`. Move them here only through a reviewed shared-contract change when another component needs a separately versioned Python package.
